从软件结构上看，视觉系统的控制程序并不是识别之后就直接把结果发出去，而是分成了初始化、主循环和状态机这几个部分，如表5-7所示。初始化部分主要完成摄像头、LCD、模型文件、串口资源和舵机资源的配置；主循环部分负责逐帧执行图像采集、FOMO 推理、候选目标后处理、状态更新、串口发送和界面显示；状态机部分则根据当前模式和目标检测结果，去组织捡球模式与击球模式下的具体流程切换。这样安排之后，图像识别、云台跟踪和通信处理都可以放在同一循环里配合完成，也能减小单帧误差直接引起动作误触发的可能性。

表5-7 控制程序主要模块


| 程序模块     | 对应函数                                                            | 主要作用                                 |
| ------------ | ------------------------------------------------------------------- | ---------------------------------------- |
| 初始化模块   | boot()、init\_camera()、init\_uart()                                | 完成设备启动、自检与资源配置             |
| 云台控制模块 | init\_servos()、update\_servo\_tracking()                           | 驱动水平与俯仰舵机执行扫描和目标跟踪     |
| 模式管理模块 | enter\_pick\_mode()、enter\_play\_mode()                            | 完成捡球模式与击球模式切换               |
| 状态机模块   | run\_state\_machine()                                               | 组织扫描、回位、确认、跟踪和反馈解锁流程 |
| 通信模块     | process\_uart\_rx()、handle\_uart\_line()、send\_runtime\_packets()、send\_command\_packet() | 负责与下位机进行串口报文收发             |

在串口通信部分，系统使用 UART3 作为视觉端与下位机之间的数据交换接口，波特率设置为 115200 bps。本文的下位机通信报文设计如表5-8所示，上电后，视觉端会先发送 hello 作为握手起始信号；当下位机返回 HI 时，程序先把通信状态从 COMM_NO_LINK 更新为 COMM_LINKED，随后再由视觉端主动回发 OK，并把链路状态更新为 COMM_OK。只有链路进入 COMM_OK，主机控制命令才允许继续执行。这样做的目的，是避免系统刚启动、串口受噪声干扰或对端尚未准备好时误触发模式切换和控制动作。

在运行过程中，视觉端会周期性向下位机发送结构化运行数据，格式为 mode, pan, tilt, distance\_cm。其中，在捡球模式下，只有当前已经进入 SEEK/TRACK 且存在有效网球目标时，才发送当前水平/俯仰舵机角度与距离估计值；在击球模式下，切入 MODE\_PLAY 后会先发送一次 PLAY,-1,-1,0.0 作为占位包，待锁定球员后再发送 PLAY,pan,tilt,0.0，用于向下位机同步当前指向角度。程序中这一过程由运行数据发送模块完成。除运行态数据帧外，系统还支持发送命令封装数据帧，格式为 cmd, arg, mode, state，用于表达当前视觉侧的命令状态和运行阶段；当击球触发条件满足时，系统还会额外发送 HIT 事件报文，用于通知下位机执行对应动作。

表5-8 下位机通信报文设计


| 报文示例                       | 方向             | 作用说明                                                       |
| ------------------------------ | ---------------- | -------------------------------------------------------------- |
| hello                          | OpenMV →下位机  | 视觉端主动发起握手                                             |
| HI                             | 下位机 → OpenMV | 表示串口链路已建立                                             |
| OK                             | OpenMV →下位机  | 表示视觉端已完成握手，可进入主控命令交互状态                   |
| SEEK,92,118,128.4              | OpenMV →下位机  | 上报捡球模式跟踪阶段的当前舵机角度与距离                       |
| PLAY,-1,-1,0.0                 | OpenMV →下位机  | 上报当前处于击球模式但暂未锁定人目标                           |
| PLAY,92,118,0.0                | OpenMV →下位机  | 上报击球模式锁人后的当前舵机角度，距离位固定为 0.0             |
| PICKED:1 / COLLECT:1           | 下位机 → OpenMV | 返回捡球执行完成反馈                                           |
| CTRL,MODE\_PLAY,REQ,T01        | 下位机 → OpenMV | 请求切换到击球模式                                             |
| ACK,MODE\_PLAY,REQ,T01         | OpenMV →下位机  | 确认已收到切换到击球模式的请求报文                             |
| CTRL,MODE\_PLAY,CONFIRM,T01    | 下位机 → OpenMV | 对切换到击球模式命令执行二次确认                               |
| ACK,MODE\_PLAY,CONFIRM,T01     | OpenMV →下位机  | 表示 MODE\_PLAY 命令已被接受                                   |
| CTRL,MODE\_PICK,REQ,T02        | 下位机 → OpenMV | 请求切换回捡球模式                                             |
| ACK,MODE\_PICK,REQ,T02         | OpenMV →下位机  | 确认已收到切换回捡球模式的请求报文                             |
| CTRL,MODE\_PICK,CONFIRM,T02    | 下位机 → OpenMV | 对切换回捡球模式命令执行二次确认                               |
| ACK,MODE\_PICK,CONFIRM,T02     | OpenMV →下位机  | 表示 MODE\_PICK 命令已被接受                                   |
| CTRL,UNLOCK\_TRACK,REQ,T03     | 下位机 → OpenMV | 请求解除当前捡球跟踪目标                                       |
| ACK,UNLOCK\_TRACK,REQ,T03      | OpenMV →下位机  | 确认已收到解除跟踪请求                                         |
| CTRL,UNLOCK\_TRACK,CONFIRM,T03 | 下位机 → OpenMV | 对跟踪解锁命令执行二次确认                                     |
| ACK,UNLOCK\_TRACK,CONFIRM,T03  | OpenMV →下位机  | 表示当前链路状态、动作条件和 token 校验均满足要求，命令已被接受 |
| HIT,1\|92\|118,PLAY,TRACK\_P   | OpenMV →下位机  | 击球触发事件报文，参数分别表示触发标志与当前舵机角度           |

为了降低串口噪声带来的误触发风险，程序中的主机控制命令采用了 REQ/CONFIRM 二次确认机制。目前支持的动作主要有 UNLOCK_TRACK、MODE_PICK 和 MODE_PLAY 三类。处理这类命令时，程序不只检查 action 与 token 是否匹配，还会继续判断通信链路是否已经进入 COMM_OK，以及当前模式是否允许执行对应动作。比如，UNLOCK_TRACK 只允许在 MODE_PICK 且 pick_substate == PICK_TRACK 时生效；MODE_PICK 只允许由击球模式切回；MODE_PLAY 则只允许由捡球模式切入。条件不满足时返回 NACK，满足时则在 REQ 阶段先返回 ACK 并记录待确认动作，在 CONFIRM 阶段再次校验 token、超时和当前状态，全部满足后才真正接受命令。也就是说，这里的通信处理不是简单收发字符串，而是把链路握手、状态判断和 token 二次确认一起考虑进去了，这样能更有效地降低误触发。

另外，视觉端还会接收下位机返回的执行反馈，比如 PICKED、PICK、PICKUP 和 COLLECT 这些消息，并在程序内部把它们转成捡球完成事件或者计数更新。这样一来，视觉端不只是单向地给下位机发送目标信息和控制状态，也会根据反馈去更新本地状态机，从而形成一定程度的闭环联动。结合捡球模式来看，视觉端在持续跟踪目标之后，并不会只依赖单一条件来结束当前流程；除了等待下位机反馈捡球完成之外，若下位机发送已确认的 UNLOCK\_TRACK 命令，或者目标在跟踪阶段连续丢失达到阈值，程序同样会回到扫描搜索状态。

从当前联调情况来看，视觉端和下位机之间已经可以完成握手、运行数据发送、执行反馈接收以及控制命令确认这些基本过程，可以支撑捡球模式和击球模式下的实际配合。对本课题现阶段的系统来说，这样的通信设计已经可以满足整机联调对稳定性的基本要求，通信代码示意如图5-19所示。

```
